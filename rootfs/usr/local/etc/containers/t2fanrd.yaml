name: t2fanrd
container:
  entrypoint: ./t2fanrd
  args:
    - "-c"
    - "./config.ini"
  security:
    writeableSysfs: true
  mounts:
    - source: /sys
      destination: /sys
      type: bind
      options:
        - bind
        - rw
restart: always
logToConsole: true
