name: t2fanrd
container:
  entrypoint: ./t2fanrd
  args:
    - "-c"
    - "./config.ini"
  security:
    writeableSysfs: true
depends:
  - network:
      - addresses
      - connectivity
restart: always
logToConsole: true
